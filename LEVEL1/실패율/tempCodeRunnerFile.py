for i in range(1, N+1):
    #     fail = 0
    #     clear = 0
    #     for user in stages:
    #         if user == i: fail += 1
    #         if user >= i: clear += 1
    #     users[i - 1].rate = fail / clear